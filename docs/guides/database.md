"# Database Guide

## Commands

- npm run db:generate
- npm run db:push
- npm run db:seed
- npm run db:studio

## Schema

Defined in src/db/schema.ts with 17 tables." | Out-File -FilePath "docs\guides\database.md" -Encoding utf8

Write-Host "database.md created" -ForegroundColor Green
